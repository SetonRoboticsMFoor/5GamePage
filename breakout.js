document.body.dataset.theme = localStorage.getItem('neon-coil-theme') || 'lime';
const canvas=document.querySelector('#game'),ctx=canvas.getContext('2d'),scoreEl=document.querySelector('#score'),statusEl=document.querySelector('#status'),startBtn=document.querySelector('#start');
let paddle,ball,bricks,score,keys={},active=false,timer;
function reset(){paddle={x:250,y:555,w:100};ball={x:300,y:530,dx:4,dy:-4};bricks=[];for(let row=0;row<5;row++)for(let col=0;col<8;col++)bricks.push({x:42+col*66,y:70+row*28,w:54,h:16,alive:true});score=0;scoreEl.textContent='000';active=true;statusEl.textContent='LIVE';startBtn.textContent='RESTART GAME';clearInterval(timer);timer=setInterval(tick,20);draw();}
function tick(){if(keys.ArrowLeft)paddle.x=Math.max(0,paddle.x-7);if(keys.ArrowRight)paddle.x=Math.min(500,paddle.x+7);ball.x+=ball.dx;ball.y+=ball.dy;if(ball.x<6||ball.x>594)ball.dx*=-1;if(ball.y<6)ball.dy*=-1;if(ball.y>545&&ball.x>paddle.x&&ball.x<paddle.x+paddle.w)ball.dy=-Math.abs(ball.dy);bricks.forEach(b=>{if(b.alive&&ball.x>b.x&&ball.x<b.x+b.w&&ball.y>b.y&&ball.y<b.y+b.h){b.alive=false;ball.dy*=-1;score+=10;scoreEl.textContent=String(score).padStart(3,'0');}});if(ball.y>600){active=false;clearInterval(timer);statusEl.textContent='GAME OVER';startBtn.textContent='PLAY AGAIN';}if(bricks.every(b=>!b.alive)){active=false;clearInterval(timer);statusEl.textContent='YOU WIN';}draw();}
function draw(){ctx.fillStyle=getComputedStyle(document.body).getPropertyValue('--board');ctx.fillRect(0,0,600,600);bricks.forEach(b=>{if(b.alive){ctx.fillStyle=getComputedStyle(document.body).getPropertyValue('--orange');ctx.fillRect(b.x,b.y,b.w,b.h);}});ctx.fillStyle=getComputedStyle(document.body).getPropertyValue('--lime');ctx.fillRect(paddle.x,paddle.y,paddle.w,12);ctx.beginPath();ctx.arc(ball.x,ball.y,8,0,Math.PI*2);ctx.fill();}document.addEventListener('keydown',e=>{keys[e.key]=true;if(e.code==='Space'&&!active)reset();});document.addEventListener('keyup',e=>keys[e.key]=false);startBtn.addEventListener('click',reset);reset();active=false;clearInterval(timer);statusEl.textContent='READY';startBtn.textContent='START GAME';
